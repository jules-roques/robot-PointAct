# OpenDrawer / eef / 4096 points, two camera views -- stage A follow-up.
# The image-conditioned counterpart of od-eef-n4096-s0; sampling fields are identical to it.
extends: _base_2views.yaml

meta:
  task: OpenDrawer
  sampling: eef
  npoints: 4096
  context: vlm
  seed: 0
  stage: "Stage 0: Two camera views"

data:
  lerobot_datasets:
    - max_npoints: 4096
      eef_sampling: true
      eef_sampling_sigma: 0.08
      eef_sampling_floor: 0.05
