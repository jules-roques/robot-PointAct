# OpenDrawer / uniform / 2048 points -- stage A.
# Uniform point subsample: the baseline draw.
extends: _base.yaml

meta:
  task: OpenDrawer
  sampling: uniform
  npoints: 2048
  context: text_cache
  seed: 0
  stage: "Stage 1: Num points & Train steps"

data:
  lerobot_datasets:
    - max_npoints: 2048
